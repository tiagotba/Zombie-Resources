
@{
    ViewBag.Title = "Recursos";
}


<h2>Recursos</h2>
<div ng-controller="RecursosController">
    <div class="panel panel-default">
        <!-- Default panel contents -->
        <div class="panel-heading">Recursos</div>
        <div class="panel-body">
            @if (HttpContext.Current.User.Identity.IsAuthenticated)
            {
                <ol class="breadcrumb">
                    <li>Bem vindo @HttpContext.Current.User.Identity.Name</li>
                    <li><a href="@Url.Action("Logoff", "Login")" class="btn btn-primary"><span class="glyphicon glyphicon-share"
      aria-hidden="true"></span>Sair</a></li>
                </ol>
            }

            <table class="table">
                <tr>
                    <th>Descrição</th>
                    <th>Qtd</th>
                    <th>Observação</th>
                    <th>Ações</th>
                </tr>
                <tr ng-repeat="recurso in listaRecursos">
                    <td ng-hide="true">{{recurso.Id}}</td>
                    <td>{{recurso.Descricao}}</td>
                    <td>{{recurso.Quantidade}}</td>
                    <td>{{recurso.Observacao}}</td>
                    <td>
                        @*<a class="btn-group btn-group-sm" ng-href="/Recursos/Edit/{{recurso.Id}}">Editar</a>*@
                        <button class="btn btn-xs btn-info" ng-click="getId(recurso.Id)"><span class="glyphicon glyphicon-pencil"
      aria-hidden="true"></span> Editar</button>
                    </td>
                    <td>
                        <button class="btn btn-xs btn-danger" ng-click="deleteRecurso(recurso.Id)"><span class="glyphicon glyphicon-remove"
      aria-hidden="true"></span>Excluir</button>
                        @*<a href="@Url.Action("Delete", "Recursos")" class="btn-group btn-group-sm">Excluir</a>*@
                    </td>
                </tr>
            </table>
        </div>
        <center>
            <ol class="breadcrumb">
                <li><a href="@Url.Action("Create", "Recursos")" class="btn btn-primary"><span class="glyphicon glyphicon-plus"
      aria-hidden="true"></span>Novo</a></li>
                <li><a href="@Url.Action("Index", "Inventario")" class="btn btn-primary"><span class="glyphicon glyphicon-list"
      aria-hidden="true"></span> Inventario</a></li>
            </ol>
        </center>
    </div>

</div>